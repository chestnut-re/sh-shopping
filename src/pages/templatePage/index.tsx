import React, { useCallback } from 'react'
import { View, Text, Button, Image } from '@tarojs/components'
import './index.less'

const TemplatePage: React.FC = () => {
  return (
    <View>
      <Button className='button'>DebugPage</Button>
    </View>
  )
}

export default TemplatePage
